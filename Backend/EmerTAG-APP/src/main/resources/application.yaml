spring:
  application:
    name: EmerTAG_BD

  datasource:
    url: jdbc:mysql://localhost:3306/EmerTAG_BD?useSSL=false&serverTimezone=America/Sao_Paulo
    username: root
    password: Saudade2231@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
